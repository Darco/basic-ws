package ${package}.persistence.dao;

import ${package}.model.${className}TO;

/**
 * Interface de servicio que define los metodos utilizados para uso especifico de ...
 * @author
 */
public interface ${className}Dao {

    ${className}TO get${className}TO(int id);

}